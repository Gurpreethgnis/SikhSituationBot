[build]
builder = "NIXPACKS"

[deploy]
startCommand = "PYTHONPATH=. gunicorn server.app:app --worker-class geventwebsocket.gunicorn.workers.GeventWebSocketWorker --workers 1 --timeout 120"
restartPolicyType = "ON_FAILURE"
