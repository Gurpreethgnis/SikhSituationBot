[build]
builder = "NIXPACKS"

[deploy]
startCommand = "PYTHONPATH=. gunicorn main:app --worker-class geventwebsocket.gunicorn.workers.GeventWebSocketWorker --workers 1 --timeout 120"
restartPolicyType = "ON_FAILURE"
