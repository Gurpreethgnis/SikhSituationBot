[phases.setup]
aptPkgs = ["ffmpeg"]

[variables]
PYTHONPATH = "."

[start]
cmd = "sh -c \"export PYTHONPATH=$PYTHONPATH:. && gunicorn main:app --worker-class geventwebsocket.gunicorn.workers.GeventWebSocketWorker --workers 1 --timeout 120\""
